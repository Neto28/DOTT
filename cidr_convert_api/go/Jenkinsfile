pipeline {
	agent any
		stages {
			stage('One') {
				steps {
					sh 'echo "Step One"'
					sh 'curl "localhost/cidr-to-mask?value=24"'
				}
			}


			stage('Two') {
				steps {
					sh 'echo "Step Two"'
				}
			} 

			stage('Three') {
				steps {
					sh 'echo "Step Three"'
				}
			}
		}
}

